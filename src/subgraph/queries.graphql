query HandlesQuery($first: Int!, $skip: Int!) {
  handles(
    first: $first
    skip: $skip
    orderBy: blockTimestamp
    orderDirection: asc
  ) {
    id
    operator
    blockNumber
    blockTimestamp
    transactionHash
    parentHandles {
      id
    }
  }
}
